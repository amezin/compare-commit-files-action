name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '37 9 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@57d51f58d78c08e511c26da2c5ddfbba693e5eeb # v2.0.3
    permissions:
      contents: read
      security-events: write
